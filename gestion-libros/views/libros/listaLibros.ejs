<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lista de Libros</title>
  <link rel="stylesheet" href="/css/style.css"> 
</head>
<body>

<div class="container mt-5">
  <h1 class="mb-4">📚 Lista de Libros</h1>

  <!-- Botón para añadir un nuevo libro -->
  <a href="/libros/nuevo" class="btn btn-primary mb-3">➕ Nuevo Libro</a>

  <!-- Tabla con los libros -->
  <table class="table table-dark table-striped text-center">
    <thead>
      <tr>
        <th>Título</th>
        <th>Autor</th>
        <th>Año</th>
        <th>Género</th>
        <th>Acciones</th>
      </tr>
    </thead>
    <tbody>
      <% libros.forEach(libro => { %>
        <tr>
          <td><%= libro?.titulo || "" %></td>
          <td><%= libro?.autor || "" %></td>
          <td><%= libro?.anio_publicacion || "" %></td>
          <td><%= libro?.genero || "" %></td>
          <td>
            <a href="/libros/<%= libro.id %>/editar" class="btn btn-sm btn-warning">✏️ Editar</a>
            <form action="/libros/<%= libro.id %>?_method=DELETE" method="POST" class="d-inline">
              <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('¿Estás seguro de eliminar este libro?')">🗑️ Eliminar</button>
            </form>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>

</body>
</html>
